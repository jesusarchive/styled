import React from 'react';
import { Alert, Button, Container, Flex, Heading } from './components';

const App: React.FC = () => {
    return (
        <div className="App">
            <Container>
                <Alert>Alert</Alert>
            </Container>
        </div>
    );
};

export default App;
