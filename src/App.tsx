import React from 'react';
import { Alert, Container } from './components';

const App: React.FC = () => {
  return (
    <div className="App">
      <Container>
        <Alert>Alert</Alert>
      </Container>
    </div>
  );
};

export default App;
